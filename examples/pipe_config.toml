# 管道配置文件示例
[pipe]
# 支持的类型: "small", "default", "large"
type = "small"
name = "my_application_pipe"

[backup_pipe]
type = "large"
name = "backup_pipe"

[performance]
# 是否启用性能监控
monitoring = true
# 日志级别
log_level = "info"